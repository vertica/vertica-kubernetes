name: Unit tests

on: [workflow_call]

jobs:

  ut:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: ./.github/actions/setup-go

    - name: Run unit tests
      run: make test
