apiVersion: vertica.com/v1
kind: VerticaDB
metadata:
  name: v-auto-cert-rotate
  annotations:
    vertica.com/enable-tls-auth: "true"
spec:
  httpsNMATLS:
    autoRotate:
      secrets:
        - cert1
        - cert2
        - cert3
